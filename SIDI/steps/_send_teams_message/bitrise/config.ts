import { IConfig } from '../../../../src/models/SidiModel';

const config: IConfig = {
  replaceIfDuplicate: true,
  mandatoryKeys: [{ key: 'TEAMS_BUILDS_WEBHOOCKS', globalValue: true }],
};

export default config;
