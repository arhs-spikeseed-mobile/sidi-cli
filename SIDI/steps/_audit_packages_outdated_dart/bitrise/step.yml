steps:
 - script@1.2.1:
    inputs:
     - content: |-
        #!/usr/bin/env bash
        set -ex
        
        dart pub outdated 
    title: "[AUDIT] - Search outdated packages"
    is_always_run: true