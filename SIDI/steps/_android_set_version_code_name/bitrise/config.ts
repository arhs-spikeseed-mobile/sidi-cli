import { IConfig } from '../../../../src/models/SidiModel';

const config: IConfig = {
  mandatoryKeys: [{ key: 'TARGET_ANDROID', globalValue: true, defaultValue: 'android/app' }],
};

export default config;
