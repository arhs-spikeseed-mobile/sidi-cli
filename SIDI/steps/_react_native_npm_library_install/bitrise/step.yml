steps:
  - script@1.1.6:
      inputs:
        - content: |-
            #!/usr/bin/env bash
            cd "$SAMPLE_APP_PATH"
      title: "[REACT-NATIVE] - Go to sample app "
  - npm@0.9.1:
      inputs:
        - command: install
      title: "[REACT-NATIVE] - NPM install for sample app"