import { IConfig } from '../../../../src/models/SidiModel';

const config: IConfig = {
  mandatoryKeys: [{ key: 'PLIST_KEY' }, { key: 'PLIST_VALUE' }, { key: 'IOS_PLIST_PATH' }],
};

export default config;
