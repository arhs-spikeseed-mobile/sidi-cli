import { IConfig } from '../../../../src/models/SidiModel';

const config: IConfig = {
  mandatoryKeys: [{ key: 'ANDROID_UNIT_TEST_CMD', defaultValue: 'testReleaseUnitTest' }],
};

export default config;
