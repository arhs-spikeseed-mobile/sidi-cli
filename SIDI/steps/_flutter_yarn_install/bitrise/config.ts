import { IConfig } from '../../../../src/models/SidiModel';

const config: IConfig = {
  mandatoryKeys: [{ key: 'PROJECT_LOCATION', globalValue: true, defaultValue: '.' }],
};

export default config;
