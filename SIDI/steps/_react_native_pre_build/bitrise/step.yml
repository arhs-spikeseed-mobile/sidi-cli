steps:
  - install-react-native@0.10.0:
      title: "[REACT-NATIVE] - Install ReactNative cli"
      inputs:
       - npm_options: "-g --force"
  - nvm@1.3.3:
      title: "[REACT-NATIVE] - Install node"
      inputs:
        - nvm_version: "$NVM_VERSION"
        - node_version: "$NODE_VERSION"